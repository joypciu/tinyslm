__pycache__/
*.pyc
.venv/
venv/
.gradio/
*.egg-info/
.DS_Store
Thumbs.db

# Keep trained weights + tokenizer in repo (needed to chat without retraining)
# Ignore local scratch only
checkpoints/*.tmp
checkpoints/memory_store.json
checkpoints/_smoke_memory.json
data/*.txt
!data/.gitkeep
benchmarks/*.log
